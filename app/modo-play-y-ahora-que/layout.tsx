import type { Metadata } from "next";
export const metadata:Metadata={title:"¿Y ahora qué? · Conversación B1 · SPANISHCUE",description:"Situaciones cotidianas, chats y consecuencias para resolver problemas hablando en español B1."};
export default function Layout({children}:{children:React.ReactNode}){return children}
