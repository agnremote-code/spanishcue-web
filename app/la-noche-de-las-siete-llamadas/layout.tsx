import type {Metadata} from "next";
import type {ReactNode} from "react";
export const metadata:Metadata={title:"La Noche de las Siete Llamadas · B1 | SPANISHCUE",description:"Clase conversacional B1 de 45 minutos para preguntar, comprender problemas, reaccionar y dar consejos justificados."};
export default function Layout({children}:{children:ReactNode}){return children;}
