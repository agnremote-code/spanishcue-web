import type {Metadata} from "next";

export const metadata:Metadata={
  title:"Estados Unidos · Coast to Coast | SPANISHCUE",
  description:"Clase interactiva A1 de conversación: 20 paradas, 120 preguntas bilingües, mapa y recursos para hablar desde el primer minuto.",
};

export default function Layout({children}:{children:React.ReactNode}){return children}
