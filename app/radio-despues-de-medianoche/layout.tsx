import type {Metadata} from "next";
import type {ReactNode} from "react";
export const metadata:Metadata={title:"Radio Después de Medianoche · Escucha B1 · SPANISHCUE",description:"Cinco historias telefónicas B1 para anticipar, reconstruir, preguntar y conversar durante 45 minutos."};
export default function Layout({children}:{children:ReactNode}){return children;}
