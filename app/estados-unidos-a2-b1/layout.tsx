import type {Metadata} from "next";

export const metadata:Metadata={
  title:"Estados Unidos · Coast to Coast A2/B1 | CHESPANISH",
  description:"La nueva edición A2/B1 de Coast to Coast: el mismo mapa 3D, 20 paradas y 120 preguntas nuevas para comparar, explicar y conversar.",
};

export default function Layout({children}:{children:React.ReactNode}){return children}
