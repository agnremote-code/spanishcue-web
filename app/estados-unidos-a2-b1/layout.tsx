import type {Metadata} from "next";

export const metadata:Metadata={
  title:"United States in Depth A2/B1 | SPANISHCUE",
  description:"Los 50 estados en un mapa 3D real, con cuatro paradas por estado, escenas animadas y 300 preguntas A2/B1.",
};

export default function Layout({children}:{children:React.ReactNode}){return children}
